# SpeedRSS – main API
# Copy to .env and set values.

# Set to true to disable x402 payment for POST /submit (for local testing). Omit or false for production.
SKIP_PAYMENT=true

# x402: receiving wallet (EVM, e.g. Base). Required when SKIP_PAYMENT is not true.
PAY_TO=0xYourAddress

# x402 facilitator (testnet: https://x402.org/facilitator; production: use mainnet facilitator URL). Required when SKIP_PAYMENT is not true.
FACILITATOR_URL=https://x402.org/facilitator

# Server port (default 4021)
PORT=4021

# SQLite DB path (default: ./data/posts.db)
DB_PATH=./data/posts.db

# Optional: SpeedRSS Telegram notifier. When set, new posts are POSTed here (run tg service, then set e.g. http://localhost:4022/notify).
# TG_NOTIFY_URL=http://localhost:4022/notify
