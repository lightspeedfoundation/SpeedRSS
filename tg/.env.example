# SpeedRSS Telegram notifier (tg)
# Copy to .env and set TELEGRAM_BOT_TOKEN.

# Create a bot via @BotFather, then paste the token here.
TELEGRAM_BOT_TOKEN=your_bot_token

# Optional: port for the notify service (default 4022).
# PORT=4022

# Optional: path to JSON file storing chat IDs (default: tg/data/chats.json). Add the bot to a channel/group and send /start to register.
# CHATS_FILE=./data/chats.json
